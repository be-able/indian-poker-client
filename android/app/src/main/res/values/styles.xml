<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@color/colorBackground</item>
        <item name="android:buttonStyle">@style/ButtonStyle</item>
        <item name="editTextStyle">@style/EditTextStyle</item>
    </style>

    <style name="ButtonStyle" parent="@android:style/Widget.Button">
        <item name="android:background">@drawable/button_default</item>
        <item name="android:textColor">@color/color_button_default_text</item>
    </style>

    <style name="EditTextStyle" parent="Widget.AppCompat.EditText">
        <item name="android:background">@drawable/edit_text_default</item>
        <item name="colorControlNormal">@color/color_edit_text_control_normal</item>
        <item name="colorControlActivated">@color/color_edit_text_control_active</item>
        <item name="android:textCursorDrawable">@color/color_edit_text_cursor</item>
        <item name="android:textColor">@color/color_edit_text_color</item>
        <item name="android:textColorHint">@color/color_edit_text_hint</item>
        <item name="android:textColorHighlight">@color/color_edit_text_highlight</item>
        <item name="android:paddingStart">20dp</item>
        <item name="android:paddingEnd">20dp</item>
    </style>

</resources>
